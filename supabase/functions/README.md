# Elistly Supabase Edge Functions

- **health** – Placeholder health check. Use for admin or other server-side logic later.
- Deploy: `supabase functions deploy health` (requires Supabase CLI and link to project).
- Set **SUPABASE_SERVICE_ROLE_KEY** in Dashboard → Project Settings → Edge Functions → Secrets. Never commit the service role key.
