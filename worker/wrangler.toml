# Elistly API Worker – deploy from Cloudflare with root directory = worker
#
# Required secret: NEON_DATABASE_URL
# Store as a Secret (encrypted): cd worker && npx wrangler secret put NEON_DATABASE_URL
name = "elistly-api"
main = "src/index.js"
compatibility_date = "2024-01-01"
